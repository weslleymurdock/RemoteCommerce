using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using RemoteCommerce.Plugins.Abstractions;

namespace {{Namespace}};

/// <summary>
/// Provides the generated entry point for the RemoteCommerce plugin.
/// </summary>
public sealed class PluginEntry : IRemoteCommercePlugin
{
    /// <summary>
    /// Registers the generated plugin services and MVC controllers.
    /// </summary>
    /// <param name="services">The host service collection.</param>
    /// <param name="manifest">The installed plugin manifest.</param>
    /// <param name="configuration">The host application configuration.</param>
    public void ConfigureServices(IServiceCollection services, PluginManifest manifest, IConfiguration configuration)
    {
        services.AddControllers().AddApplicationPart(typeof(PluginEntry).Assembly);

        // Use configuration here to read host defaults or plugin-specific settings.
        _ = configuration;
    }
}
